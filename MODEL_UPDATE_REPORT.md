# Word2Vec 项目更新与模型实验报告

## 一、项目更新概述

本次更新围绕训练可靠性、模型可配置性和实验对比能力展开。在原有 PyTorch Word2Vec（SGNS）项目基础上，完成了以下改进：

| 改进方向 | 主要内容 |
|---|---|
| 代码简化 | 精简重复的边界判断与负采样流程 |
| 训练控制 | 增加验证集、最佳模型保存和 early stopping |
| 模型结构 | 支持双 embedding table 与共享 embedding table |
| 超参数配置 | 支持自定义 embedding 维度 |
| 打分方式 | 支持 dot 和 cosine 两种训练方式 |
| 模型管理 | 根据超参数生成独立 checkpoint 名称 |
| 联合测试 | 支持多个模型同时返回同一查询词的 top-10 结果 |

相关功能在独立开发分支中完成，测试通过后已合并到 `main`。当前版本包含6项自动测试，覆盖模型模式、数据划分、负采样、温度缩放及 checkpoint 加载等关键行为。

## 二、核心修改说明

### 1. 验证集与 early stopping

数据按完整句子划分为训练集和验证集，避免同一句话生成的上下文同时进入两个集合。训练期间每轮计算验证 loss，并只保存当前最佳模型；若验证 loss 连续多轮没有明显改善，则提前结束训练。

这项改动使模型选择不再只依赖训练 loss，也减少了无效训练和过拟合风险。

### 2. 可切换的 embedding 结构

模型现在支持两种结构：

- `dual`：中心词和上下文分别使用一张 embedding table；
- `shared`：中心词和上下文共享一张 embedding table。

双表结构具有更强的表达能力，共享结构参数更少。词向量维度也可以通过参数直接设置。

### 3. 可切换的训练打分方式

训练支持点积（dot）和余弦相似度（cosine）两种打分方式。cosine 模式加入 temperature 参数，用于调整分数范围。本次实验最终采用 `temperature=0.1`。

### 4. 多模型保存与联合推理

checkpoint 名称包含主要超参数，不同实验可以分别保存，避免互相覆盖。推理程序支持一次加载多个 checkpoint，因此输入一个单词后，可以直接比较不同模型返回的 top-10 相似词。

## 三、训练中发现的问题及修正

### 问题一：验证集出现错误的负样本

初版验证流程只依据各自子集判断正负关系，导致训练集中已经出现过的真实上下文，有可能在验证集中被当成负样本。其结果是训练 loss 下降，而验证 loss 异常上升，并很快触发 early stopping。

**修正方法：** 使用完整语料建立统一的正上下文关系，训练集和验证集在生成负样本时都排除这些已知正样本。同时固定验证集负样本，确保不同 epoch 的验证结果可以稳定比较。

修正后，10K句冒烟实验的验证 loss 从第1轮 `2.762801` 降至第2轮 `2.740203`，验证过程恢复正常。

### 问题二：cosine 模型发生向量坍塌

最初直接使用未缩放的 cosine 分数训练。程序和 loss 都没有报错，但查询结果中大量单词的相似度接近 `0.9998`，且出现许多与查询词无关的结果。这说明词向量方向变得过于接近，模型失去了区分语义的能力。

**修正方法：** 为 cosine 分数加入 temperature 缩放，并将其设置为 `0.1`，以扩大正负样本之间的分数差异。

修正后，相似度回到约 `0.5～0.7` 的正常区间，`music`、`city` 和 `war` 的相似词重新集中到对应主题。未缩放的 cosine 模型因此被排除在正式对比之外。

## 四、正式实验与结果

### 1. 实验设置

四个正式模型均使用100K句英文 Wikipedia 语料，主要参数保持一致：

| 参数 | 设置 |
|---|---:|
| 词向量维度 | 100 |
| 窗口半径 | 5 |
| 负样本数 | 5 |
| batch size | 2,048 |
| 学习率 | 0.01 |
| 最大 epoch | 20 |
| 验证集比例 | 10% |
| patience | 3 |
| 随机种子 | 42 |
| 训练设备 | CUDA |

### 2. 训练结果

| 模型 | 最佳 epoch | 最佳验证 loss | 停止情况 |
|---|---:|---:|---|
| dual + dot | 1 | 2.458485 | 第4轮 early stop |
| shared + dot | 6 | 3.739783 | 第9轮 early stop |
| dual + cosine，T=0.1 | 20 | 2.250856 | 完成20轮 |
| shared + cosine，T=0.1 | 20 | 3.799480 | 完成20轮 |

不同结构和打分方式的 loss 不完全处于同一尺度，因此上表主要用于观察各模型自身的训练过程，不能只按 loss 数值直接评判模型优劣。

### 3. 相似词对比

推理阶段统一使用 cosine 相似度。三个查询词的结果概括如下：

| 模型 | `music` | `city` | `war` | 综合观察 |
|---|---|---|---|---|
| dual + dot | musical, folk, solo | town, park, area | military, peace, civil | 结果合理，但较分散 |
| shared + dot | film, musical, series | town, downtown, street | military, army, battle | `war` 较好，其他主题偏泛 |
| dual + cosine，T=0.1 | pop, folk, jazz | town, county, district | army, civil, campaign | 三个主题都最集中 |
| shared + cosine，T=0.1 | dance, songs, albums | town, area, county | army, military, troops | 整体较好，偶尔出现噪声 |

## 五、结论

本次更新完成了模型结构、向量维度和打分方式的参数化，加入了验证集与 early stopping，并实现了多模型独立保存和联合测试。训练过程中发现的验证负样本冲突和 cosine 向量坍塌问题，也分别通过全局正上下文排除和 temperature 缩放得到解决。

基于 `music`、`city` 和 `war` 三组人工测试，`dual + cosine + temperature=0.1` 的整体表现最好，返回的相似词最集中；`shared + cosine + temperature=0.1` 在减少参数的情况下也取得了较好的效果。

当前结论来自少量查询词的定性比较。后续可加入公开的词相似度或词类比数据集，使用统一指标进行定量评估，从而更客观地判断不同模型的泛化能力。
